# Generated release metadata is intentionally managed by Release Please.
CHANGELOG.md

# Runtime files are checked out under .github during CI. Composite actions
# are copied to .github/actions for the workflow run and must not be policed
# by consumer formatters (their style follows the runtime baseline, not the
# consumer .prettierrc).
.github/.code-foundry
.github/actions/

# Repository-specific rules
# Runtime files are checked out under .github during CI.
.next
.turbo
.venv
**/.venv
**/.docusaurus
**/.next
**/build
**/coverage
**/next-env.d.ts
**/node_modules
**/dist
**/.app
apps/api/api/config
.code-foundry
**/public
# Archived leaderboard snapshots are machine-generated data; keep them compact
# so the server-only lazy imports do not carry formatting overhead.
apps/app/src/constants/leaderboards/*.json
pnpm-lock.yaml
.vscode/
.vercel/
