// Keep the marketing site as the stable entry point so Vercel can forward the
// path to the app host without coupling site content to the app's origin.
export const NIFTY_APP_URL = '/app'
export const NIFTY_WORLD_APP_URL = '/app/world'
