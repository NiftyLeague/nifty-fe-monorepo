import type { Metadata } from 'next';
import type { PropsWithChildren } from 'react';
import { customFontClassName } from '@nl/ui/fonts';

import '@/styles/app.css';

export const metadata: Metadata = { title: 'Create Turborepo', description: 'Generated by create turbo' };

export default function RootLayout({ children }: PropsWithChildren): React.ReactNode {
  return (
    <html lang="en" className={customFontClassName}>
      <body suppressHydrationWarning className="dark">
        {children}
      </body>
    </html>
  );
}
