# Operating system
.DS_Store
Thumbs.db
.Trashes

# Secrets and local environment
.env
.env.*
!.env.example
*.pem
*.key

# JavaScript and TypeScript
node_modules/
.pnp/
.pnp.js
.turbo/
*.tsbuildinfo

# Python
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Rust
target/

# Build and test output
dist/
build/
out/
coverage/
htmlcov/
.coverage*
artifacts/
performance-results.json
performance-results/
cache/
!.github/actions/cache/
!.github/actions/cache/action.yml
typechain/
coverage.json
.code-foundry/
tests/.bin/
**/.app/

# Generated SEO files belong to the app that serves them, never to the shared
# assets directory. Every Astro app uses ../../assets as its publicDir, so a file
# left here is copied into each app's output: stale staging files from the
# pre-migration build sat here and silently replaced apps/web's own sitemap in
# local builds.
assets/robots.txt
assets/sitemap.xml
assets/sitemap-*.xml
assets/sitemap-index.xml

# Logs and temporary files
*.log
*.tmp
*.pid
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Editors, agents, and local tools
.claude/
.kilo/
.codex/
.cursor/
.windsurf/
.vscode/
.idea/
.zed/
.fleet/
.opencode/
.zcode/
.kluster/
.direnv/
.envrc
.vercel/
mise.local.toml
.mise.local.toml
mise.local.lock
.mise.local.lock
*.code-workspace
